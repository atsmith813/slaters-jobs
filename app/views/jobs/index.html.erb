<div class="container">
	<% @jobs.each do |job| %>
		<div class="panel panel-default">
			<div class ="panel-body">
				<%= link_to job.title, job %> - <i> published <%= time_ago_in_words(job.published) %> ago. </i>
			</div>
		</div>
	<% end %>
</div>
